# Astra — Security & Data Flow Model

## Zero-Trust Security Architecture

```mermaid
flowchart TB
    subgraph EXTERNAL["External"]
        USER["User / Browser"]
        CI["CI/CD Pipeline"]
    end
    
    subgraph PERIMETER["Perimeter"]
        WAF["WAF / DDoS Protection\nCloudflare / AWS WAF"]
        TLS["TLS 1.3\nEnd-to-end encryption"]
    end
    
    subgraph CONTROL["Control Plane"]
        AUTH["Auth Layer\nSSO · SAML · OIDC\nAPI Key validation\nRBAC enforcement"]
        API["API Gateway\nRate limiting\nRequest validation\nAudit logging"]
        
        subgraph MODULES["Modules"]
            M1["Findings\nEncrypted at rest"]
            M2["Policies\nOrg-scoped"]
            M3["Users\nHashed passwords\nMFA support"]
        end
    end
    
    subgraph DATA["Data Plane"]
        DP["Scanner Agent\nRuns in isolated container"]
        OLL["Ollama\nLocal AI inference\nNo code leaves"]
    end
    
    subgraph STORAGE["Storage"]
        PG[("PostgreSQL\nAES-256 encryption\nRow-level security")]
        S3[("S3 / MinIO\nServer-side encryption\nVersioning")]
    end
    
    USER -->|"HTTPS"| WAF --> TLS --> AUTH --> API
    CI -->|"HTTPS + Token"| WAF --> TLS --> API
    API --> MODULES --> PG
    API --> S3
    
    DP -->|"HTTPS + Token\nFindings only"| API
    DP --> OLL
    
    style EXTERNAL fill:#f4f4f4,stroke:#161616
    style PERIMETER fill:#e0e0e0,stroke:#161616
    style CONTROL fill:#ffffff,stroke:#0f62fe,stroke-width:2px
    style DATA fill:#ffffff,stroke:#42be65,stroke-width:2px
    style STORAGE fill:#f4f4f4,stroke:#161616
```

---

## Data Flow — What Leaves the Customer Environment

```mermaid
graph LR
    subgraph CUSTOMER["Customer Environment"]
        REPO["Source Code\nNEVER leaves"]
        DP["Data Plane Agent"]
    end
    
    subgraph TRANSIT["Transit"]
        FINDINGS["Normalized Findings JSON\nHTTPS + TLS 1.3\nGzip compressed"]
    end
    
    subgraph ASTRA["Astra Control Plane"]
        CP["Control Plane"]
        DB[("PostgreSQL"]
    end
    
    REPO -->|"scan"| DP
    DP -->|"extract"| FINDINGS
    FINDINGS -->|"POST /v1/ingest"| CP
    CP --> DB
    
    style REPO fill:#fa4d56,color:#fff
    style FINDINGS fill:#42be65,color:#fff
```

### What Gets Transmitted

| Data | Leaves Customer? | Notes |
|------|------------------|-------|
| Raw source code | ❌ NO | Stays in Data Plane container |
| File paths | ✅ YES | Relative paths only |
| Line numbers | ✅ YES | For locating findings |
| Code snippets | ✅ YES | 5-10 lines around finding only |
| Scanner metadata | ✅ YES | Rule IDs, severity, categories |
| AI explanations | ✅ YES | Generated by AI, not raw code |
| AI fix suggestions | ✅ YES | Generated code snippets |
| Business logic rules | ✅ YES | Inferred rules, not raw code |
| Scan artifacts | ❌ NO | Stored in customer S3/MinIO |

---

## Authentication & Authorization

```mermaid
flowchart TB
    subgraph AUTH["Authentication Flow"]
        USER["User / CI"]
        
        subgraph METHODS["Auth Methods"]
            SAML["SAML 2.0\nEnterprise SSO"]
            OIDC["OpenID Connect\nGoogle · Azure AD · Okta"]
            API_KEY["API Key\nFor CI/CD integration"]
            TOKEN["Scan Token\nShort-lived JWT"]
        end
        
        subgraph RBAC["Role-Based Access Control"]
            ADMIN["Admin\nFull access"]
            SEC_OPS["Sec Ops\nTriage + policies"]
            ENG["Engineer\nView findings + fix"]
            VIEWER["Viewer\nRead-only dashboard"]
        end
        
        USER --> METHODS
        METHODS --> RBAC
    end
    
    subgraph SCOPE["Multi-Tenancy"]
        ORG1["Org A\nRow-level isolation"]
        ORG2["Org B\nRow-level isolation"]
        ORG3["Org C\nRow-level isolation"]
    end
    
    RBAC -->|"org-scoped queries"| SCOPE
```

### Permission Matrix

| Action | Admin | Sec Ops | Engineer | Viewer |
|--------|-------|---------|----------|--------|
| Trigger scan | ✅ | ✅ | ❌ | ❌ |
| View findings | ✅ | ✅ | ✅ | ✅ |
| Triage finding | ✅ | ✅ | ❌ | ❌ |
| Create policy | ✅ | ✅ | ❌ | ❌ |
| Manage integrations | ✅ | ✅ | ❌ | ❌ |
| View AI fix | ✅ | ✅ | ✅ | ❌ |
| Export reports | ✅ | ✅ | ✅ | ❌ |
| Manage users | ✅ | ❌ | ❌ | ❌ |
| Configure AI models | ✅ | ❌ | ❌ | ❌ |

---

## Audit Logging

```mermaid
flowchart LR
    subgraph EVENTS["Audit Events"]
        E1["scan.triggered"]
        E2["finding.viewed"]
        E3["finding.triaged"]
        E4["policy.changed"]
        E5["user.login"]
        E6["integration.configured"]
    end
    
    subgraph LOG["Audit Log"]
        STRUCT["Structured JSON\n{ timestamp, actor, action,\n  resource, before, after,\n  ip_address, user_agent }"]
    end
    
    subgraph RETENTION["Retention"]
        HOT["Hot: 30 days\nPostgreSQL"]
        COLD["Cold: 7 years\nS3 Glacier"]
    end
    
    EVENTS --> STRUCT
    STRUCT --> HOT
    HOT --> COLD
```

---

## Compliance Posture

| Requirement | Implementation |
|-------------|----------------|
| **Data Residency** | Self-hosted option keeps all data on-prem |
| **Encryption at Rest** | PostgreSQL AES-256, S3 SSE-S3 |
| **Encryption in Transit** | TLS 1.3 for all connections |
| **Access Logging** | Every API call logged with actor + timestamp |
| **Data Retention** | Configurable per-org (30 days to 7 years) |
| **Right to Deletion** | Full org data purge via admin API |
| **SOC 2 Type II** | Roadmap: Q3 2026 |
| **ISO 27001** | Roadmap: Q4 2026 |
